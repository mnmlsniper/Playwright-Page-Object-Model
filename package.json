{
  "name": "pom",
  "version": "0.0.1",
  "scripts": {
    "allureGenerate": "allure generate --clean",
    "allureOpen": "allure open",
    "api": "npx playwright test --grep @API",
    "debug": "npx playwright test --ui",
    "test": "npx playwright test",
    "testEnv": "export NODE_ENV=auto-test npx playwright test"
  },
  "keywords": [],
  "author": "mnmlsniper",
  "license": "ISC",
  "description": "Playwright-Page-Object-Model",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.46.1",
    "@types/node": "^22.5.0",
    "allure-playwright": "^3.0.0",
    "os": "^0.1.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
